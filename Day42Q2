# Q84
s = "hello world"
upper_str = ""

for ch in s:
    if 'a' <= ch <= 'z':  
        # Convert by subtracting 32 from ASCII code
        upper_str += chr(ord(ch) - 32)
    else:
        upper_str += ch

print(upper_str)
