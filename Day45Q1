#include <stdio.h>

int main() {
    char str[] = "programming";
    char ch = 'g';
    int count = 0;

    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] == ch)
            count++;
    }

    printf("Character '%c' appears %d times.\n", ch, count);
    return 0;
}
