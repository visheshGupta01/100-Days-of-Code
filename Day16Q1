#include <stdio.h>
int main() {
    int n, rem, bin = 0, place = 1;
    printf("Enter a number: ");
    scanf("%d", &n);
    while (n > 0) {
        rem = n % 2;
        bin += rem * place;
        place *= 10;
        n /= 2;
    }
    printf("Binary = %d\n", bin);
    return 0;
}
